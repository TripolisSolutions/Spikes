$("#flash").html("<% flash.each do |key, value| %><%=  escape_javascript(content_tag(:div, value, class: "alert alert-#{key}")) %><% end
flash.discard %>");
$("#flash").show(100).delay(800).fadeOut(2500);
$("#timeline").delay(5000).html("<%= escape_javascript(render :partial => 'home_page/tweets') %>");
