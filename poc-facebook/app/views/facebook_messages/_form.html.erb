<%= form_for(@facebook_message) do |f| %>
    <% if @facebook_message.errors.any? %>
        <div class="alert alert-error">
          <strong><%= pluralize(@facebook_message.errors.count, "error") %> prohibited this page from being saved:</strong>

          <ul>
            <% @facebook_message.errors.full_messages.each do |msg| %>
                <li><%= msg %></li>
            <% end %>
          </ul>
        </div>
    <% end %>

    <%= f.label :message %><br />
    <%= f.text_area :message ,:style=>"height:200px"%>

  <div class="actions">
    <%= f.submit 'Submit' ,:class => "btn"%>
  </div>
<% end %>
